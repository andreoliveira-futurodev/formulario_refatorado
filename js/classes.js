export class Endereco {
  constructor(cep, logradouro, bairro, cidade, estado, numero) {
    this.cep = cep;
    this.logradouro = logradouro;
    this.bairro = bairro;
    this.cidade = cidade;
    this.estado = estado;
    this.numero = numero;
  }
}
